services:
   run-tests:
     image: automationexercise-image:latest
     container_name: automationexercise_container
     volumes:
      - ./:/usr/workspace
     command: bash -c "pytest -n 3 --alluredir=allure-results"